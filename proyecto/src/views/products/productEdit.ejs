<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../partials/head") %>
        <title>Carrito</title>
    </head>
    <body>
        <%- include("../partials/header") %> <%-
        include("../partials/slide-show")%>
        <main id="main-content">
            <h2>Edición de producto</h2>
            <form action="/product/edit/<%= producto.id%>?_method=PATCH" method="POST" class="flex justify-space-around" enctype="multipart/form-data">
                <article
                    class="
                        flex-detail
                        product-detail
                        align-center
                        justify-space-around
                        w-100
                    "
                >
                    <img
                        class="product-img-detail"
                        src=<%= producto.img %> 
                        alt="3060 Ti"
                    />

                    <div class="flex direction-column">
                        <label class="form-label">Nombre producto:</label>
                        <input class="form-input" type="text" name="title" value="<%= producto.title %>">
                        <label class="form-label">Tipo de producto:</label>
                        <select class="form-input" name="tipe">
                            <option value="Procesador">Procesador</option>
                            <option value="Tarjeta Gráfica">Tarjeta Gráfica</option>
                            <option value="Monitor">Monitor</option>
                            <option value="Teclado Mecanico">Teclado Mecanico</option>
                        </select>
                        <label class="form-label">Precio del producto</label>
                        <input class="form-input" type="text" name="price" value="<%= producto.price %>">
                        <label class="form-label">Imagen producto:</label>
                        <input class="form-input" type="file" name="img" accept=".jpg">

                        <h1>Descripción</h1>

                        <textarea name="desc" ><%= producto.desc %></textarea>
                        <button type="submit">Enviar</button>
                    </div>
                </article>
            </form>
        </main>
        <%- include("../partials/footer") %>
    </body>
</html>
